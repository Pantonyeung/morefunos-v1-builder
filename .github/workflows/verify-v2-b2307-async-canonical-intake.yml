name: V2 B2307 Async Canonical Intake

on:
  push:
    paths:
      - 'requests/v2-b2307-async-canonical-intake-request.txt'

permissions:
  contents: read

jobs:
  verify:
    if: github.actor == 'Pantonyeung'
    runs-on: ubuntu-24.04
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v7
        with: {persist-credentials: false}
      - name: Resolve exact source
        id: request
        shell: bash
        run: |
          set -euo pipefail
          SOURCE_SHA="$(sed -n 's/^source_sha=//p' requests/v2-b2307-async-canonical-intake-request.txt | tr -d '[:space:]' | tr '[:upper:]' '[:lower:]')"
          [[ "$SOURCE_SHA" =~ ^[0-9a-f]{40}$ ]]
          echo "source_sha=$SOURCE_SHA" >> "$GITHUB_OUTPUT"
      - name: Checkout exact V2 source
        uses: actions/checkout@v7
        with:
          repository: Pantonyeung/Morefun-v2
          ref: ${{ steps.request.outputs.source_sha }}
          token: ${{ secrets.V2_SOURCE_READ_TOKEN }}
          path: source
          persist-credentials: false
      - name: Verify exact source identity
        env:
          EXPECTED: ${{ steps.request.outputs.source_sha }}
        shell: bash
        run: |
          set -euo pipefail
          test "$(git -C source rev-parse HEAD)" = "$EXPECTED"
      - uses: actions/setup-node@v7
        with: {node-version: '24'}
      - name: Install source dependencies
        working-directory: source
        run: npm install --no-save --ignore-scripts --no-audit --no-fund
      - name: Root TypeScript gate
        working-directory: source
        run: npm run typecheck
      - name: Store Order Intake async seam gate
        working-directory: source
        run: npx vitest run packages/store-order-intake/store-order-intake.test.ts packages/store-order-intake/store-order-intake-async.test.ts
      - name: D1-backed canonical Order acceptance gate
        working-directory: source
        run: node --test packages/order/d1-store-order-acceptance.test.ts
      - name: D1 Keeta durable ingress gate
        working-directory: source
        run: node --test packages/integration/d1-keeta-durable-runtime.test.ts
      - name: Keeta durable canonical delivery gate
        working-directory: source
        run: node --test packages/integration/keeta-durable-delivery.test.ts packages/integration/keeta-canonical-order-delivery.test.ts
