name: Keeta V2 Order 1001 Runtime RED

on:
  push:
    branches: [main]
    paths:
      - '.ci-requests/keeta-v2-order-1001-runtime-red.sha'

permissions:
  contents: read

jobs:
  red:
    if: github.actor == 'Pantonyeung'
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    env:
      SOURCE_SHA: '99a2fed8424d144820eb38e0fbe35e390df353aa'
    steps:
      - uses: actions/checkout@v7
        with:
          persist-credentials: false
      - name: Guard exact request
        shell: bash
        run: |
          set -euo pipefail
          test "$(tr -d '[:space:]' < .ci-requests/keeta-v2-order-1001-runtime-red.sha)" = "$SOURCE_SHA"
      - uses: actions/checkout@v7
        with:
          repository: Pantonyeung/Morefun-v2
          ref: ${{ env.SOURCE_SHA }}
          token: ${{ secrets.V2_SOURCE_READ_TOKEN }}
          path: source
          persist-credentials: false
          fetch-depth: 1
      - uses: actions/setup-node@v7
        with:
          node-version: '24'
      - name: Install and run exact Keeta regression
        working-directory: source
        run: |
          npm install --ignore-scripts --no-audit --no-fund
          npm run typecheck:intg-keeta
          npm run test:intg-keeta
