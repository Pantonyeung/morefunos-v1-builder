name: Owner V1 Verify A Line

on:
  workflow_dispatch:
    inputs:
      source_sha:
        description: 'Exact 40-character MoreFunOS V1 A candidate SHA'
        required: true
        type: string
  workflow_call:
    inputs:
      source_sha:
        required: true
        type: string

permissions:
  contents: read

concurrency:
  group: morefunos-v1-a-verify
  cancel-in-progress: false

jobs:
  verify-a:
    if: github.actor == 'Pantonyeung'
    uses: ./.github/workflows/verify-core.yml
    with:
      source_sha: ${{ inputs.source_sha }}
      verification_profile: 'a'
    secrets: inherit
