name: V2 CMD-053 Shared DIRECT Final Gate

on:
  push:
    branches: [main]
    paths: ['requests/v2-cmd053-shared-direct-final-gate-request.txt']

permissions:
  contents: read

jobs:
  verify:
    if: github.actor == 'Pantonyeung'
    runs-on: ubuntu-24.04
    timeout-minutes: 30
    steps:
      - name: Checkout Builder definition
        uses: actions/checkout@v7
        with:
          persist-credentials: false

      - name: Resolve exact request
        id: request
        shell: bash
        run: |
          set -euo pipefail
          SOURCE_SHA="$(sed -n 's/^source_sha=//p' requests/v2-cmd053-shared-direct-final-gate-request.txt | tr -d '[:space:]' | tr '[:upper:]' '[:lower:]')"
          BRANCH_NAME="$(sed -n 's/^branch_name=//p' requests/v2-cmd053-shared-direct-final-gate-request.txt | tr -d '\r\n')"
          [[ "$SOURCE_SHA" =~ ^[0-9a-f]{40}$ ]]
          [[ -n "$BRANCH_NAME" ]]
          echo "source_sha=$SOURCE_SHA" >> "$GITHUB_OUTPUT"
          echo "branch_name=$BRANCH_NAME" >> "$GITHUB_OUTPUT"

      - name: Require V2 source credential
        env:
          TOKEN: ${{ secrets.V2_SOURCE_READ_TOKEN }}
        run: test -n "$TOKEN"

      - name: Checkout exact Morefun-v2 source
        uses: actions/checkout@v7
        with:
          repository: Pantonyeung/Morefun-v2
          ref: ${{ steps.request.outputs.source_sha }}
          token: ${{ secrets.V2_SOURCE_READ_TOKEN }}
          path: source
          persist-credentials: false
          fetch-depth: 0

      - name: Verify exact source identity
        env:
          EXPECTED: ${{ steps.request.outputs.source_sha }}
        run: |
          set -euo pipefail
          mkdir -p evidence
          test "$(git -C source rev-parse HEAD)" = "$EXPECTED"
          echo "source-identity=PASS sha=$EXPECTED" | tee evidence/source-identity.log

      - name: Setup Node 24
        uses: actions/setup-node@v7
        with:
          node-version: '24'

      - name: Install TypeScript test toolchain
        working-directory: source
        run: npm install --no-save --ignore-scripts --no-audit --no-fund typescript@5.9.2 @types/node@22

      - name: Run Pricing tests
        working-directory: source
        shell: bash
        run: |
          set -euo pipefail
          node --test --experimental-strip-types packages/pricing/*.test.ts 2>&1 | tee ../evidence/pricing-tests.log

      - name: Run Catalog tests
        working-directory: source
        shell: bash
        run: |
          set -euo pipefail
          node --test --experimental-strip-types packages/catalog/*.test.ts 2>&1 | tee ../evidence/catalog-tests.log

      - name: Run Business Day unit tests
        working-directory: source
        shell: bash
        run: |
          set -euo pipefail
          mapfile -t tests < <(find packages/business-day-cash -maxdepth 1 -name '*.test.ts' ! -name '*.integration.test.ts' -print | sort)
          test "${#tests[@]}" -gt 0
          node --test --experimental-strip-types "${tests[@]}" 2>&1 | tee ../evidence/business-day-tests.log

      - name: Run shared DIRECT final gate tests
        working-directory: source
        shell: bash
        run: |
          set -euo pipefail
          node --test --experimental-strip-types packages/order/direct-final-gate.test.ts packages/store-order-intake/store-order-intake.test.ts packages/store-order-intake/store-order-intake-async.test.ts 2>&1 | tee ../evidence/direct-final-gate-tests.log

      - name: Run root TypeScript typecheck
        working-directory: source
        shell: bash
        run: |
          set -euo pipefail
          ./node_modules/.bin/tsc --noEmit -p tsconfig.json 2>&1 | tee ../evidence/root-typecheck.log

      - name: Write evidence
        if: always()
        shell: bash
        env:
          SOURCE_SHA: ${{ steps.request.outputs.source_sha }}
        run: |
          mkdir -p evidence
          printf 'source_repo=Pantonyeung/Morefun-v2\nsource_sha=%s\nworkflow=v2-cmd053-shared-direct-final-gate.yml\n' "$SOURCE_SHA" > evidence/summary.txt

      - name: Upload evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: v2-cmd053-${{ steps.request.outputs.source_sha }}
          path: evidence
          if-no-files-found: error
          retention-days: 30
