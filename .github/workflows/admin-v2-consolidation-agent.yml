name: Admin V2 Consolidation Agent

on:
  push:
    paths:
      - '.github/workflows/admin-v2-consolidation-agent.yml'
  workflow_dispatch:

permissions:
  contents: write

env:
  SOURCE_SHA: 212cdf22e7ff49ffe4ce9d4c66cbe1ea8169e793

jobs:
  admin-consolidation:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/setup-node@v7
        with:
          node-version: '22'
      - uses: actions/checkout@v7
        with:
          repository: Pantonyeung/morefunos-v1
          ref: ${{ env.SOURCE_SHA }}
          token: ${{ secrets.V1_SOURCE_READ_TOKEN }}
          path: source
          persist-credentials: false
          fetch-depth: 1
      - name: Verify exact source
        run: test "$(git -C source rev-parse HEAD)" = "$SOURCE_SHA"
      - name: Install Admin Web dependencies
        working-directory: source/apps/admin-web
        run: npm install --legacy-peer-deps --ignore-scripts --no-audit --no-fund
      - name: Full Admin Web tests
        working-directory: source/apps/admin-web
        run: npm test
      - name: Admin Web typecheck and build
        working-directory: source/apps/admin-web
        run: npm run build
      - name: Persist diagnostic
        if: always()
        env:
          GH_TOKEN: ${{ github.token }}
          JOB_STATUS: ${{ job.status }}
        run: |
          path='diagnostics/admin-v2-consolidation-latest.log'
          printf 'SOURCE_SHA=%s\nJOB_STATUS=%s\nWORKFLOW_RUN_ID=%s\nWORKFLOW_RUN_ATTEMPT=%s\n' "$SOURCE_SHA" "$JOB_STATUS" "$GITHUB_RUN_ID" "$GITHUB_RUN_ATTEMPT" > /tmp/result.log
          content="$(base64 -w0 /tmp/result.log)"
          current_sha="$(gh api "repos/${GITHUB_REPOSITORY}/contents/${path}?ref=${GITHUB_REF_NAME}" --jq '.sha' 2>/dev/null || true)"
          args=(--method PUT "repos/${GITHUB_REPOSITORY}/contents/${path}" -f message='ci: refresh Admin consolidation diagnostic' -f content="$content" -f branch="$GITHUB_REF_NAME")
          if [ -n "$current_sha" ]; then args+=(-f sha="$current_sha"); fi
          gh api "${args[@]}" >/dev/null
