name: Keeta 1008 Hotfix Verify

on:
  push:
    branches: [main]
    paths:
      - '.ci-requests/keeta-1008-hotfix.sha'

permissions:
  contents: read

jobs:
  verify:
    if: github.actor == 'Pantonyeung'
    runs-on: ubuntu-24.04
    timeout-minutes: 12
    steps:
      - uses: actions/checkout@v7
        with: { persist-credentials: false }
      - name: Resolve V2 SHA
        id: src
        shell: bash
        run: |
          set -euo pipefail
          SHA="$(tr -d '[:space:]' < .ci-requests/keeta-1008-hotfix.sha)"
          [[ "$SHA" =~ ^[0-9a-f]{40}$ ]]
          echo "sha=$SHA" >> "$GITHUB_OUTPUT"
      - uses: actions/checkout@v7
        with:
          repository: Pantonyeung/Morefun-v2
          ref: ${{ steps.src.outputs.sha }}
          token: ${{ secrets.V2_SOURCE_READ_TOKEN }}
          path: source
          persist-credentials: false
          fetch-depth: 1
      - uses: actions/setup-node@v7
        with: { node-version: '24' }
      - name: Install
        working-directory: source
        run: npm install --ignore-scripts --no-audit --no-fund
      - name: Targeted 1008 regression
        working-directory: source
        run: node --test packages/integration/keeta-webhook-ingress.test.ts
      - name: Keeta integration regression
        working-directory: source
        run: node --test packages/integration/keeta*.test.ts
      - name: Root typecheck
        working-directory: source
        run: npm run typecheck
