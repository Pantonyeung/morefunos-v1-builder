name: Keeta Order Get Commercial Readback
on:
  push:
    branches: [main]
    paths: ['.ci-requests/keeta-order-get-commercial-readback.sha']
permissions:
  contents: read
jobs:
  verify:
    runs-on: ubuntu-24.04
    timeout-minutes: 8
    steps:
      - uses: actions/checkout@v7
        with: { persist-credentials: false }
      - name: Resolve SHA
        id: src
        shell: bash
        run: |
          set -euo pipefail
          SHA="$(tr -d '[:space:]' < .ci-requests/keeta-order-get-commercial-readback.sha)"
          [[ "$SHA" =~ ^[0-9a-f]{40}$ ]]
          echo "sha=$SHA" >> "$GITHUB_OUTPUT"
      - uses: actions/checkout@v7
        with:
          repository: Pantonyeung/Morefun-v2
          ref: ${{ steps.src.outputs.sha }}
          token: ${{ secrets.V2_SOURCE_READ_TOKEN }}
          path: source
          persist-credentials: false
          fetch-depth: 1
      - uses: actions/setup-node@v7
        with: { node-version: '24' }
      - name: Install
        working-directory: source
        run: npm install --ignore-scripts --no-audit --no-fund
      - name: Targeted tests
        working-directory: source
        run: |
          node --test packages/integration/keeta-order-get-commercial-readback.test.ts
          node --test packages/integration/keeta-order-commercial-snapshot.test.ts
      - name: Full typecheck
        working-directory: source
        run: npm run typecheck
