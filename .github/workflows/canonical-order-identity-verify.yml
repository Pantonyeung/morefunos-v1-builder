name: Canonical Order Identity Verify
on:
  push:
    branches: [main]
    paths: ['.ci-requests/canonical-order-identity.sha']
permissions:
  contents: read
jobs:
  verify:
    runs-on: ubuntu-24.04
    timeout-minutes: 8
    steps:
      - uses: actions/checkout@v7
        with: { persist-credentials: false }
      - name: Resolve SHA
        id: src
        shell: bash
        run: |
          set -euo pipefail
          SHA="$(tr -d '[:space:]' < .ci-requests/canonical-order-identity.sha)"
          [[ "$SHA" =~ ^[0-9a-f]{40}$ ]]
          echo "sha=$SHA" >> "$GITHUB_OUTPUT"
      - uses: actions/checkout@v7
        with:
          repository: Pantonyeung/Morefun-v2
          ref: ${{ steps.src.outputs.sha }}
          token: ${{ secrets.V2_SOURCE_READ_TOKEN }}
          path: source
          persist-credentials: false
      - uses: actions/setup-node@v7
        with: { node-version: '24' }
      - name: Install
        working-directory: source
        run: npm install --ignore-scripts --no-audit --no-fund
      - name: Canonical identity tests
        working-directory: source
        run: |
          node --test packages/order/canonical-order-identity.test.ts
          node --test packages/order/order-display-code.test.ts
          node --test packages/order/d1-order-display-code.test.ts
          node --test packages/order/d1-acceptance-replay.test.ts
          node --test packages/integration/keeta-canonical-order-projection.test.ts
          node --test apps/smm-web/src/smm-user-messages.test.ts
          npx vitest run apps/smt-clean/src/runtime/network-production-runtime.test.ts
          node --test packages/integration/keeta-standard-canonical-acceptance-bridge.test.ts
          node --test packages/integration/keeta-d1-canonical-acceptance-plan.test.ts
          node --test packages/smt-ui-integration/trusted-worker-order-projection.test.ts
          node --test apps/smm-web/src/smm-user-messages.test.ts
      - name: Targeted Order identity typecheck
        working-directory: source
        run: tsc --noEmit --skipLibCheck --module NodeNext --moduleResolution NodeNext --target ES2022 --allowImportingTsExtensions packages/order/canonical-order-identity.ts packages/order/order-display-code.ts packages/order/d1-order-display-code.ts packages/order/d1-acceptance.ts packages/order/d1-store-order-acceptance.ts packages/order/d1-customer-order-service.ts packages/integration/keeta-canonical-order-projection.ts
      - name: Full repo typecheck baseline signal
        working-directory: source
        continue-on-error: true
        run: npm run typecheck
