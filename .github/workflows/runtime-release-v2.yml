name: Runtime Release V2 Retired

on:
  workflow_call:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-24.04
    steps:
      - name: Runtime Release V2 is retired
        shell: bash
        run: |
          echo 'Runtime Release V2 is retired. Use Runtime Release V3.' >&2
          exit 1
