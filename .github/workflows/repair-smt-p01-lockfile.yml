name: Repair SMT P01 Lockfile

on:
  issue_comment:
    types: [created]
  workflow_call:
    inputs:
      source_sha:
        required: true
        type: string
    outputs:
      new_sha:
        description: 'Source SHA after canonical SMT lockfile writeback'
        value: ${{ jobs.repair.outputs.new_sha }}
      lock_sha256:
        description: 'Canonical SMT package-lock SHA-256'
        value: ${{ jobs.repair.outputs.lock_sha256 }}
    secrets:
      V1_SOURCE_READ_TOKEN:
        required: true
      V1_B2_LOCKFILE_WRITE_TOKEN:
        required: true

permissions:
  contents: read
  issues: write

jobs:
  repair:
    if: >-
      github.actor == 'Pantonyeung' &&
      (
        inputs.source_sha != '' ||
        (github.event.issue.number == 2 && startsWith(github.event.comment.body, '/repair-smt-p01-lockfile '))
      )
    runs-on: ubuntu-latest
    timeout-minutes: 20
    outputs:
      new_sha: ${{ steps.writeback.outputs.new_sha }}
      lock_sha256: ${{ steps.lock.outputs.lock_sha256 }}
    env:
      COMMENT_BODY: ${{ github.event.comment.body }}
      INPUT_SOURCE_SHA: ${{ inputs.source_sha }}
      TARGET_BRANCH: agent/smt-p01-owner-locked-implementation-20260820
    steps:
      - name: Validate immutable repair request
        id: request
        shell: bash
        run: |
          set -euo pipefail
          if [[ -n "${INPUT_SOURCE_SHA:-}" ]]; then
            SOURCE_SHA="$(printf '%s' "$INPUT_SOURCE_SHA" | tr -d '[:space:]')"
          else
            COMMAND="$(printf '%s' "$COMMENT_BODY" | tr -d '\r' | sed -e 's/^[[:space:]]*//' -e 's/[[:space:]]*$//')"
            SOURCE_SHA="${COMMAND#'/repair-smt-p01-lockfile '}"
            SOURCE_SHA="$(printf '%s' "$SOURCE_SHA" | tr -d '[:space:]')"
            test "$COMMAND" = "/repair-smt-p01-lockfile ${SOURCE_SHA}"
          fi
          [[ "$SOURCE_SHA" =~ ^[0-9a-fA-F]{40}$ ]]
          echo "source_sha=${SOURCE_SHA,,}" >> "$GITHUB_OUTPUT"

      - name: Setup Node 22
        uses: actions/setup-node@v7
        with:
          node-version: '22'

      - name: Checkout exact private source SHA
        uses: actions/checkout@v7
        with:
          repository: Pantonyeung/morefunos-v1
          ref: ${{ steps.request.outputs.source_sha }}
          token: ${{ secrets.V1_SOURCE_READ_TOKEN }}
          path: source
          persist-credentials: false

      - name: Verify exact source identity
        shell: bash
        run: |
          set -euo pipefail
          test "$(git -C source rev-parse HEAD)" = "${{ steps.request.outputs.source_sha }}"
          test -z "$(git -C source status --porcelain)"

      - name: Canonicalize SMT lockfile and prove npm ci
        id: lock
        shell: bash
        run: |
          set -euo pipefail
          cd source
          test -f apps/smt-web/package.json
          rm -rf apps/smt-web/node_modules
          rm -f apps/smt-web/package-lock.json
          npm --prefix apps/smt-web install --package-lock-only --ignore-scripts --no-audit --no-fund
          test -f apps/smt-web/package-lock.json
          node -e "JSON.parse(require('node:fs').readFileSync('apps/smt-web/package-lock.json','utf8'))"
          npm --prefix apps/smt-web ci --ignore-scripts --no-audit --no-fund
          rm -rf apps/smt-web/node_modules
          test "$(git diff --name-only)" = "apps/smt-web/package-lock.json"
          test -z "$(git ls-files --others --exclude-standard)"
          LOCK_SHA256="$(sha256sum apps/smt-web/package-lock.json | awk '{print $1}')"
          echo "lock_sha256=$LOCK_SHA256" >> "$GITHUB_OUTPUT"

      - name: Commit only lockfile and fast-forward isolated implementation branch
        id: writeback
        shell: bash
        env:
          V1_WRITE_TOKEN: ${{ secrets.V1_B2_LOCKFILE_WRITE_TOKEN }}
          SOURCE_SHA: ${{ steps.request.outputs.source_sha }}
        run: |
          set -euo pipefail
          test -n "$V1_WRITE_TOKEN"
          cd source
          REMOTE_HEAD="$(git ls-remote https://x-access-token:${V1_WRITE_TOKEN}@github.com/Pantonyeung/morefunos-v1.git refs/heads/${TARGET_BRANCH} | awk '{print $1}')"
          test "$REMOTE_HEAD" = "$SOURCE_SHA"
          git config user.name 'MoreFunOS V1 Builder'
          git config user.email '107616897+Pantonyeung@users.noreply.github.com'
          git add -- apps/smt-web/package-lock.json
          test "$(git diff --cached --name-only)" = "apps/smt-web/package-lock.json"
          git commit -m 'build(smt): canonicalize P01 web lockfile'
          NEW_SHA="$(git rev-parse HEAD)"
          ASKPASS="$RUNNER_TEMP/v1-smt-lock-write-askpass.sh"
          cat > "$ASKPASS" <<'EOF'
          #!/bin/sh
          case "$1" in
            *Username*) echo "x-access-token" ;;
            *Password*) printf '%s\n' "$V1_WRITE_TOKEN" ;;
            *) exit 1 ;;
          esac
          EOF
          chmod 700 "$ASKPASS"
          export GIT_ASKPASS="$ASKPASS"
          export GIT_TERMINAL_PROMPT=0
          git push https://github.com/Pantonyeung/morefunos-v1.git "HEAD:refs/heads/${TARGET_BRANCH}"
          rm -f "$ASKPASS"
          echo "new_sha=$NEW_SHA" >> "$GITHUB_OUTPUT"

      - name: Report sanitized result
        if: ${{ always() && !cancelled() && github.event_name == 'issue_comment' }}
        shell: bash
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          SOURCE_SHA: ${{ steps.request.outputs.source_sha }}
          NEW_SHA: ${{ steps.writeback.outputs.new_sha }}
          LOCK_SHA256: ${{ steps.lock.outputs.lock_sha256 }}
          REQUEST_OUTCOME: ${{ steps.request.outcome }}
          LOCK_OUTCOME: ${{ steps.lock.outcome }}
          WRITEBACK_OUTCOME: ${{ steps.writeback.outcome }}
        run: |
          set -euo pipefail
          if [[ -n "${NEW_SHA:-}" ]]; then
            BODY="smt-p01-lockfile-repair ${SOURCE_SHA} PASS new_sha=${NEW_SHA} lock_sha256=${LOCK_SHA256}"
          else
            if [[ "${REQUEST_OUTCOME:-}" != 'success' ]]; then
              STAGE='request'
            elif [[ "${LOCK_OUTCOME:-}" == 'failure' ]]; then
              STAGE='lock_or_npm_ci'
            elif [[ "${LOCK_OUTCOME:-}" != 'success' ]]; then
              STAGE='checkout_or_identity_before_lock'
            elif [[ "${WRITEBACK_OUTCOME:-}" == 'failure' ]]; then
              STAGE='writeback'
            else
              STAGE='unknown'
            fi
            BODY="smt-p01-lockfile-repair ${SOURCE_SHA:-UNKNOWN} FAIL stage=${STAGE} request=${REQUEST_OUTCOME:-UNKNOWN} lock=${LOCK_OUTCOME:-UNKNOWN} writeback=${WRITEBACK_OUTCOME:-UNKNOWN}"
          fi
          echo "$BODY"
          gh issue comment 2 --body "$BODY" >/dev/null
