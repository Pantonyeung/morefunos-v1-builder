name: V2 Governance Verification

on:
  workflow_dispatch:
    inputs:
      source_sha:
        description: Exact Morefun-v2 source SHA
        required: true
        type: string
      base_sha:
        description: Optional exact comparison base SHA
        required: false
        type: string
      branch_name:
        description: V2 branch name represented by source SHA
        required: true
        default: main
        type: string
  push:
    paths:
      - 'requests/v2-governance-request.txt'

permissions:
  contents: read
  statuses: write

jobs:
  verify:
    if: github.actor == 'Pantonyeung'
    runs-on: ubuntu-24.04
    timeout-minutes: 15
    steps:
      - name: Publish Builder pending status
        env:
          GH_TOKEN: ${{ github.token }}
          SHA: ${{ github.sha }}
        run: |
          gh api --method POST "/repos/${{ github.repository }}/statuses/$SHA" \
            -f state=pending \
            -f context='builder/v2-governance' \
            -f description='Builder verification started'

      - name: Checkout Builder definition
        uses: actions/checkout@v7
        with:
          persist-credentials: false

      - name: Mark governance request pending
        env:
          GH_TOKEN: ${{ github.token }}
          SHA: ${{ github.sha }}
        run: gh api --method POST "/repos/${{ github.repository }}/statuses/$SHA" -f state=pending -f context='builder/v2-governance/request' -f description='Governance request validation running'

      - name: Resolve exact request
        id: request
        shell: bash
        env:
          EVENT_NAME: ${{ github.event_name }}
          INPUT_SOURCE_SHA: ${{ inputs.source_sha }}
          INPUT_BASE_SHA: ${{ inputs.base_sha }}
          INPUT_BRANCH_NAME: ${{ inputs.branch_name }}
        run: |
          set -euo pipefail
          if [[ "$EVENT_NAME" == "workflow_dispatch" ]]; then
            SOURCE_SHA="$(printf '%s' "$INPUT_SOURCE_SHA" | tr -d '[:space:]' | tr '[:upper:]' '[:lower:]')"
            BASE_SHA="$(printf '%s' "${INPUT_BASE_SHA:-}" | tr -d '[:space:]' | tr '[:upper:]' '[:lower:]')"
            BRANCH_NAME="$(printf '%s' "$INPUT_BRANCH_NAME" | tr -d '\r\n')"
          else
            test -f requests/v2-governance-request.txt
            SOURCE_SHA="$(sed -n 's/^source_sha=//p' requests/v2-governance-request.txt | tr -d '[:space:]' | tr '[:upper:]' '[:lower:]')"
            BASE_SHA="$(sed -n 's/^base_sha=//p' requests/v2-governance-request.txt | tr -d '[:space:]' | tr '[:upper:]' '[:lower:]')"
            BRANCH_NAME="$(sed -n 's/^branch_name=//p' requests/v2-governance-request.txt | tr -d '\r\n')"
          fi
          [[ "$SOURCE_SHA" =~ ^[0-9a-f]{40}$ ]]
          if [[ -n "$BASE_SHA" ]]; then [[ "$BASE_SHA" =~ ^[0-9a-f]{40}$ ]]; fi
          [[ -n "$BRANCH_NAME" ]]
          {
            echo "source_sha=$SOURCE_SHA"
            echo "base_sha=$BASE_SHA"
            echo "branch_name=$BRANCH_NAME"
          } >> "$GITHUB_OUTPUT"
          echo "request source_sha=$SOURCE_SHA base_sha=${BASE_SHA:-none} branch_name=$BRANCH_NAME"

      - name: Mark governance request success
        if: ${{ steps.request.outcome == 'success' }}
        env:
          GH_TOKEN: ${{ github.token }}
          SHA: ${{ github.sha }}
        run: gh api --method POST "/repos/${{ github.repository }}/statuses/$SHA" -f state=success -f context='builder/v2-governance/request' -f description='Governance request validation passed'

      - name: Mark governance request failure
        if: ${{ failure() && steps.request.outcome == 'failure' }}
        env:
          GH_TOKEN: ${{ github.token }}
          SHA: ${{ github.sha }}
        run: gh api --method POST "/repos/${{ github.repository }}/statuses/$SHA" -f state=failure -f context='builder/v2-governance/request' -f description='Governance request validation failed'

      - name: Require V2 source credential
        shell: bash
        env:
          TOKEN: ${{ secrets.V2_SOURCE_READ_TOKEN }}
        run: |
          set -euo pipefail
          test -n "$TOKEN"
          echo "v2-source-credential=present"

      - name: Checkout exact Morefun-v2 source
        uses: actions/checkout@v7
        with:
          repository: Pantonyeung/Morefun-v2
          ref: ${{ steps.request.outputs.source_sha }}
          token: ${{ secrets.V2_SOURCE_READ_TOKEN }}
          path: source
          fetch-depth: 0
          persist-credentials: false

      - name: Verify exact source identity
        shell: bash
        env:
          EXPECTED: ${{ steps.request.outputs.source_sha }}
        run: |
          set -euo pipefail
          test "$(git -C source rev-parse HEAD)" = "$EXPECTED"
          echo "source-identity=PASS sha=$EXPECTED"

      - name: Mark stale-room guard pending
        env:
          GH_TOKEN: ${{ github.token }}
          SHA: ${{ github.sha }}
        run: gh api --method POST "/repos/${{ github.repository }}/statuses/$SHA" -f state=pending -f context='builder/v2-governance/stale-room' -f description='Stale-room guard running'

      - name: Verify stale Room pointer hard guard
        id: stale_room
        shell: bash
        run: |
          set -euo pipefail
          cd source
          python3 -m unittest scripts/test_governance_guard_stale_room_pointer.py

      - name: Mark stale-room guard success
        if: ${{ steps.stale_room.outcome == 'success' }}
        env:
          GH_TOKEN: ${{ github.token }}
          SHA: ${{ github.sha }}
        run: gh api --method POST "/repos/${{ github.repository }}/statuses/$SHA" -f state=success -f context='builder/v2-governance/stale-room' -f description='Stale-room guard passed'

      - name: Mark stale-room guard failure
        if: ${{ failure() && steps.stale_room.outcome == 'failure' }}
        env:
          GH_TOKEN: ${{ github.token }}
          SHA: ${{ github.sha }}
        run: gh api --method POST "/repos/${{ github.repository }}/statuses/$SHA" -f state=failure -f context='builder/v2-governance/stale-room' -f description='Stale-room guard failed'

      - name: Mark governance guard pending
        if: ${{ steps.stale_room.outcome == 'success' }}
        env:
          GH_TOKEN: ${{ github.token }}
          SHA: ${{ github.sha }}
        run: gh api --method POST "/repos/${{ github.repository }}/statuses/$SHA" -f state=pending -f context='builder/v2-governance/guard' -f description='Governance guard running'

      - name: Run Morefun-v2 governance guard
        id: governance_guard
        shell: bash
        env:
          SOURCE_SHA: ${{ steps.request.outputs.source_sha }}
          BASE_SHA: ${{ steps.request.outputs.base_sha }}
          BRANCH_NAME: ${{ steps.request.outputs.branch_name }}
        run: |
          set -euo pipefail
          ARGS=(--head "$SOURCE_SHA" --branch "$BRANCH_NAME")
          if [[ -n "$BASE_SHA" ]]; then ARGS+=(--base "$BASE_SHA"); fi
          (
            cd source
            python3 scripts/governance_guard.py "${ARGS[@]}"
          ) | tee "$RUNNER_TEMP/v2-governance-guard.log"

      - name: Mark governance guard success
        if: ${{ steps.governance_guard.outcome == 'success' }}
        env:
          GH_TOKEN: ${{ github.token }}
          SHA: ${{ github.sha }}
        run: gh api --method POST "/repos/${{ github.repository }}/statuses/$SHA" -f state=success -f context='builder/v2-governance/guard' -f description='Governance guard passed'

      - name: Mark governance guard failure
        if: ${{ failure() && steps.governance_guard.outcome == 'failure' }}
        env:
          GH_TOKEN: ${{ github.token }}
          SHA: ${{ github.sha }}
        run: gh api --method POST "/repos/${{ github.repository }}/statuses/$SHA" -f state=failure -f context='builder/v2-governance/guard' -f description='Governance guard failed'

      - name: Write evidence manifest
        if: always()
        shell: bash
        env:
          SOURCE_SHA: ${{ steps.request.outputs.source_sha }}
          BASE_SHA: ${{ steps.request.outputs.base_sha }}
          BRANCH_NAME: ${{ steps.request.outputs.branch_name }}
        run: |
          set -euo pipefail
          cat > "$RUNNER_TEMP/v2-governance-evidence.txt" <<EOF
          builder_repo=Pantonyeung/morefunos-v1-builder
          builder_run_id=${GITHUB_RUN_ID}
          source_repo=Pantonyeung/Morefun-v2
          source_sha=${SOURCE_SHA}
          base_sha=${BASE_SHA:-none}
          branch_name=${BRANCH_NAME}
          EOF
          if [[ -f "$RUNNER_TEMP/v2-governance-guard.log" ]]; then
            cat "$RUNNER_TEMP/v2-governance-guard.log" >> "$RUNNER_TEMP/v2-governance-evidence.txt"
          fi

      - name: Upload governance evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: v2-governance-evidence-${{ steps.request.outputs.source_sha }}
          path: |
            ${{ runner.temp }}/v2-governance-evidence.txt
            ${{ runner.temp }}/v2-governance-guard.log
          if-no-files-found: error
          retention-days: 30


      - name: Publish Builder success status
        if: success()
        env:
          GH_TOKEN: ${{ github.token }}
          SHA: ${{ github.sha }}
        run: |
          gh api --method POST "/repos/${{ github.repository }}/statuses/$SHA" \
            -f state=success \
            -f context='builder/v2-governance' \
            -f description='Builder verification passed'

      - name: Publish Builder failure status
        if: failure()
        env:
          GH_TOKEN: ${{ github.token }}
          SHA: ${{ github.sha }}
        run: |
          gh api --method POST "/repos/${{ github.repository }}/statuses/$SHA" \
            -f state=failure \
            -f context='builder/v2-governance' \
            -f description='Builder verification failed'
